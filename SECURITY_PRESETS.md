# Security Preset Options

Presets control auto-approvals for execution tiers:

1. `safe`: Auto-approves nothing but read-only tasks.
2. `standard`: Auto-approves harmless writing, but gates CLI and remote pushes.
