# Daemon Workspace Configuration

## Getting Started

1. To run the daemon workspace watcher in development mode:
   ```bash
   npm run dev --filter=daemon
   ```

2. To build production assets:
   ```bash
   npm run build --filter=daemon
   ```
