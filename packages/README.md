# Packages Workspace

This directory contains shared packages utilized across the monorepo workspace apps:

## Workspace Packages

- `core/`: Application state management, syncing logic, orchestration, and worker queues.
- `db-schema/`: Dextro Database Schema representations, definitions, and database drivers.
